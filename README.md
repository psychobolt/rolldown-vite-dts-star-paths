# Minimal Reproduction for rolldown-vite DTS plugin "*" paths issue

This project reproduces the issue with rolldown-plugin-dts `"*"` paths in TypeScript configuration

## Instructions

1. Install dependencies: `npm install`
2. Build: `npm run (vite|rolldown)-build`
